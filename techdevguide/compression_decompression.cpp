#include <bits/stdc++.h>

using namespace std;

string decompose(string in) {
    char first = in[0];
    if (isdigit(first)) {
        return 
    } else if (isalpha(first)) {

    } else {

    }
    return in;
}

int main() {
    string in;
    // cin >> in;
    in = "3[abc]4[ab]c";
    cout << decompose(in) << endl;
}